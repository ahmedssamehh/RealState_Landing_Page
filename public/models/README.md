Drop a `villa.glb` here and pass it to the hero scene:

    <HeroScene controls={controls} modelUrl="/models/villa.glb" />

The generated villa is skipped entirely; drag, inertia, auto-rotate, parallax
and the scroll response all keep working against the imported model.
